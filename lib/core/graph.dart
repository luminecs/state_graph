/// Graph is a simple implementation of a bi-directed
/// graph using adjacency list. It can automatically
/// delete nodes which become zero out-degree.
class Graph<T> {
  // Graph({this.name})

  /// Get name from the node.
  // final String Function(T)? name;
  // String _name(T node) => name != null ? name!(node) : node.toString();
}